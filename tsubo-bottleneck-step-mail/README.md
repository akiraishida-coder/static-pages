# 坪田まゆみ｜ボトルネック診断 ステップメール（静的HTML）

- 元データ: `11_受講生サポート/PLCコミュニティ/受講生用/tsubo_坪田/20260404_コンサル販売/ステップメール/*.md`
- 再生成: theleadpromotion リポジトリルートで  
  `python3 ツール/開発支援/tsubo_step_mail_md_to_html.py`

## クライアントに渡す本番URL（GitHub Pages）

公開は **theleadpromotion ではなく、別リポジトリ `static-pages`** から行う（栗林×橋本などと同じ `tanakayuc.github.io/static-pages/`）。

**目次（ここを渡す）**  
https://tanakayuc.github.io/static-pages/tsubo-bottleneck-step-mail/index.html

push 直後は反映に **1〜2分** かかることがある。

## 本番へ載せる手順（static-pages リポジトリ）

1. 本スクリプトで `docs/static_notes/tsubo-bottleneck-step-mail/` を更新する  
2. そのフォルダを `static-pages` リポジトリの `tsubo-bottleneck-step-mail/` に同期（例）:

```bash
rsync -a --delete \
  "/path/to/theleadpromotion/docs/static_notes/tsubo-bottleneck-step-mail/" \
  "/path/to/static-pages/tsubo-bottleneck-step-mail/"
```

3. `static-pages` で `git add` → `commit` → `push`（ブランチ `main`）

## 共有方法の例（補足）

- **ZIP で渡す** … 本フォルダごと zip し、`index.html` を開いてもらう  
- **Netlify Drop 等** … フォルダをドロップで公開

---
Produced by KATSUO MARKETING
Version: 1.1 (2026-04-04)
Licensed to: KATSUO MARKETING Community Member
Security Trace ID: H8T9M0K2
Generated by: 田中祐一
---
